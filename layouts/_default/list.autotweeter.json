{{ $ppl := .GetPage "expertise" }}
[
  {{ range $i, $page := $ppl.Pages }}
    {{ if ne $i 0 }},{{ end }}
    {
      "id": {{ .Permalink|jsonify }},
      "url": {{ .Permalink|jsonify }},
      "title": {{ .LinkTitle|jsonify }},
      "count": {{ len .Pages | printf "%d" |jsonify }}
    }
  {{ end }}
]
